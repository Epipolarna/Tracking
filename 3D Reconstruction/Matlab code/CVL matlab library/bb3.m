% function [r,x] = bb3(y)
%
% Generates a new x= x_k+1= f({x_i},y_k), i<=k and a corresponding reward
% r= r({x_i},y_k), i<=k. Your mission is again to generate y and obtain  
% sum(r) > 100.
%
% The dimension of x and y should be 4 x 1 and 1 x 1 respectively.
%
% If you get the error message "Logging failed, exiting..." please
% contact tc@isy.liu.se or knutte@isy.liu.se