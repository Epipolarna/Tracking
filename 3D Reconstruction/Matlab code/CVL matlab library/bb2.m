% function [r,x] = bb2(y)
%
% Generates a new x= x_k+1= f({x_i},{y_i}), i<=k and a corresponding reward
% r= r(x_k,y_k). Your mission is to generate y and obtain  sum(r) > 100.
%
% The dimension of x and y should be 2 x 1 and 20 x 1 respectively.
%
% If you get the error message "Logging failed, exiting..." please
% contact tc@isy.liu.se or knutte@isy.liu.se